export default function SettingsPage() {
  return (
    <main className="mx-auto w-full max-w-2xl flex-1 overflow-y-auto p-6">
      <h1 className="text-xl font-semibold tracking-tight">Settings</h1>
      <p className="mt-1 text-sm text-[var(--muted)]">Nothing here yet.</p>

      <div className="mt-6 rounded-xl border border-dashed border-white/10 bg-white/[0.02] p-10 text-center text-sm text-[var(--muted)]">
        Settings coming soon.
      </div>
    </main>
  );
}
