import { describe, it, expect } from "vitest";

// Smoke test: confirms the test runner is wired up and the `@/` alias resolves.
// This is the placeholder test for Part 0; real suites replace it.
describe("test harness", () => {
  it("runs and asserts", () => {
    expect(1 + 1).toBe(2);
  });
});
